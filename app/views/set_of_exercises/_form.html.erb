<%= form_for(@set_of_exercise) do |f| %>
  <% if @set_of_exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@set_of_exercise.errors.count, "error") %> prohibited this set_of_exercise from being saved:</h2>

      <ul>
      <% @set_of_exercise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :exercise_id %><br>
    <%= f.text_field :exercise_id %>
  </div>
  <div class="field">
    <%= f.label :weight %><br>
    <%= f.number_field :weight %>
  </div>
  <div class="field">
    <%= f.label :reps %><br>
    <%= f.number_field :reps %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
